/**
 * Pretty shitty way of handling errors right now; we'll get around to fixing this.
 * @return {Object} Object containing an error message.
 */
export default function() {
    return {
        message: 'Oops, there was an error!'
    };
};
